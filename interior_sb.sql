CREATE TABLE contact (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(20),
    plan VARCHAR(20),
    email VARCHAR(20),
    number VARCHAR(15),
    address VARCHAR(30),
    message VARCHAR(30),
    gender VARCHAR(10)
);
